# Database (required for backend and migrations)
# Local:
DATABASE_URL=postgresql://localhost:5432/learning_buddy
# External (dcdeploy) - use after running migrate-to-remote-db.sh:
# DATABASE_URL=postgresql://user:pass@host:port/database-db
# Password can contain { } ] etc.; no encoding needed when set in .env or via single-quoted env var

# Test database (run migrations and tests against this)
DATABASE_URL_TEST=postgresql://localhost:5432/learning_buddy_test

# JWT (required for Phase 2 auth)
JWT_SECRET=your-secret-min-32-chars-for-signing

# OpenAI API key for extraction (images → GPT-4o Vision, PDFs → pdf-parse in Node). Required for upload → notes.
# OPENAI_API_KEY=sk-proj-...

# Port (default 3000). Use 3001 if another app uses 3000.
# PORT=3001
